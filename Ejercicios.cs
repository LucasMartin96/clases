namespace Projecto25
{
    public class Ejercicios
    {
        public void Run()
        {
            /*
                Crear una clase llamada Pais con las propiedades 
                Nombre : string
                Poblacion : int
                Capital: string
                latCapital: float
                longCapital: float
                Continente: string
                AnioFundacion: int
                
                Metodos:
                CalcularAniosDelPais : calcula los anios que tiene el pais(devuelve un int)
                CalcularPoblacionAXAnios: formula (poblacion * 0.12) * x + poblacion
                    Descripcion: Siendo 0.12 la variacion ANUAL de poblacion, pasar por parametro la cantidad de anios
                    a calcular(int), y devolver la resultado de la formula
                
            */
            
            // Crear una lista que contenga objetos del tipo Pais
            
            // Mostrar por consola los nombres de todos los paises
            
            // Mostrar por consola los años de todos los paises
            
            // Mostrar por consola la poblacion calculada a 7 anios de todos los paises
            
            // Mostrar por consola el nombre de los paises que tengan mas de 50 mill de poblacion calculada a 10
            
        }
    }
}